export default function Footer() {
  return (
    <footer className="footer">
      <div className="container" style={{ display: "flex", justifyContent: "space-between", flexWrap: "wrap", gap: 12, width: "100%" }}>
        <span>8WAVES CONNECT — OSTOTORONGO / FACULTY signal hub</span>
        <span>Built for our class, one wave at a time</span>
      </div>
    </footer>
  );
}
